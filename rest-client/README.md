This folder contains simple REST client examples for the hello2026 project.

Usage:
- Start the NestJS server: `npm run start:dev`
- Use the example.http file with the VS Code REST Client extension or any HTTP tool (Postman / cURL).

Files:
- [example.http](example.http) — example requests for the `Products` endpoints. Click this link in VS Code's Markdown preview to open the file inside the editor.

How to open in VS Code:
- Open this README and click the link above in the Markdown preview (press `Ctrl+Shift+V`).
- Or open the file from the Explorer: rest-client/example.http
